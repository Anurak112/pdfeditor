/// <reference types="svelte" />
/// <reference types="vite/client" />

/** Injected by vite.config.ts from package.json — see `define`. */
declare const __APP_VERSION__: string;
/** Date the running bundle was built (YYYY-MM-DD). */
declare const __BUILD_DATE__: string;
/** False in the single-file build, which has no sw.js beside it to register. */
declare const __HAS_SW__: boolean;
